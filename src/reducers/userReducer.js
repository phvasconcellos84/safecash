const initialState = {
    user: '',
    password: ''
}

export default (state = initialState, action) => {

    switch (action.data) {
        case 'ADD_USER':
            break;
    }

    return state;
}