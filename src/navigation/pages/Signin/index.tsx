import React from 'react';
import {
    View,
    StyleSheet,
    Text,
} from 'react-native';

const Signin = () => {
    return (
        <View>
            <Text>Signin</Text>
        </View>
    );
}

export default Signin;

const styles = StyleSheet.create({
});